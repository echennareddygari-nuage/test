## GENERATED BY KICKSTART ANSIBLE
## DO NOT MODIFY FOR ANY REASON

projectName = "{{ projectName }}"
internalFQDN = "{{ internalFQDN }}"
eksClusterName = "{{ eksClusterName }}"

awsAccessKey = "{{ aws.accessKey }}"
awsSecretKey = "{{ aws.secretKey }}"
awsRegion = "{{ aws.region }}"
awsAZ1 = "{{ aws.AZ1 }}"
awsAZ2 = "{{ aws.AZ2 }}"
awsAZ3 = "{{ aws.AZ3 }}"

awsKeyName = "{{ aws.keyName }}"

metalInstanceType = "{{ aws.instanceTypes.metal }}"
proxyInstanceType = "{{ aws.instanceTypes.proxy }}"
eksInstanceType = "{{ aws.instanceTypes.eks }}"
jumpInstanceType = "{{ aws.instanceTypes.jump }}"
toolsInstanceType = "{{ aws.instanceTypes.tools }}"

metalAMI = "{{ aws.ami.metal }}"
proxyAMI = "{{ aws.ami.proxy }}"
jumpAMI = "{{ aws.ami.jump }}"
toolsAMI = "{{ aws.ami.tools }}"

eksVersion = "{{ aws.eks.version }}"        
eksWorkerAMI = "{{ aws.eks.workerAMI }}"

